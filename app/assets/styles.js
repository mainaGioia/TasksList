import { StyleSheet, Platform } from 'react-native';

export default StyleSheet.create({
    container: {
        padding: 10,
    },
    textInput: {
    },
    listItem: {
        padding: 10,
        fontSize: 18,
        height: 44,
      },
});


